#include <iostream>
using namespace std;


int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);

    // 입력
    long long a = 0, b = 0, c = 0;
    cin >> a >> b >> c;
    
    cout << a + b + c << '\n';
    return 0;
}
